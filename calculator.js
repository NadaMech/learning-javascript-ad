"use strict"; 
//functions will be read below here

function adder(number1,number2)
{
    let add1 = prompt("You've called the adder function- insert first number here", ); //the numbers will stand for the first, second, third, etc number you punch in

    let add2 = prompt("Instert the next number here", );
    
    return alert(parseint(add1) + parseint(add2));
};

adder();